"use client";

// Trang danh sách Thuộc tính (/admin/categories/attributes)
// Tái sử dụng component `AttributeList.tsx`.

import AttributeList from "../AttributeList";

export default function AttributesPage() {
  return <AttributeList />;
}

