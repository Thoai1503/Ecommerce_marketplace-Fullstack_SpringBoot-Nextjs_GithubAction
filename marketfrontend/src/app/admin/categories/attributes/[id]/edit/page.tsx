"use client";

// Route động cho chỉnh sửa thuộc tính (/admin/categories/attributes/[id]/edit)
// Tái sử dụng form `EditAttribute.tsx`.

import EditAttribute from "../../../EditAttribute";

export default function EditAttributePage() {
  return <EditAttribute />;
}

