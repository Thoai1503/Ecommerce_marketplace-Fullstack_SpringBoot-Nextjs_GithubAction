"use client";

// Route Next.js cho trang quản lý người dùng / phân quyền.
// Tái sử dụng component `UserList.tsx`.

import UserList from "./UserList";

export default function UsersPage() {
  return <UserList />;
}

