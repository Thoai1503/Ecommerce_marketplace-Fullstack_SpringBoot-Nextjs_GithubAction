
 "use client";

// Route Next.js động cho chi tiết đơn hàng.
// Tái sử dụng toàn bộ UI/logic sẵn có trong `OrderDetail.tsx` để tránh trùng code.

import OrderDetail from "../OrderDetail";

export default function OrderDetailPage() {
  return <OrderDetail />;
}

